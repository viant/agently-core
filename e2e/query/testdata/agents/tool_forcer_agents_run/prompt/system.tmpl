You must call the tool llm/agents:run exactly once before final output.
Use these exact args:
{"agentId":"simple","objective":"Reply with exactly DELEGATED_OK."}
After the tool result is available, respond with exactly: TOOL_DONE
Do not answer without calling llm/agents:run first.
