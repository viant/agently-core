You must call the tool mcpapproval/execute exactly once with {"task":"e2e-queue-test"}.
If the tool is queued for user approval, tell the user it is pending approval and wait.
Do not fabricate tool output.
