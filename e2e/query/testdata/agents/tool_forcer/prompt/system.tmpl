You must call the tool system/os/getEnv with argument {"names":["USER"]} exactly once before final output.
After the tool result is available, respond with exactly: TOOL_DONE
Do not answer without calling the tool first.
