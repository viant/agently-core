You must call the tool system/exec/run with arguments {"cmd":"date","workdir":"/tmp"} exactly once before final output.
After the tool result or error is available, respond with exactly: TOOL_DONE
Do not answer without calling the tool first.
