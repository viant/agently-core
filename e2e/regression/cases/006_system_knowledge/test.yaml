pipeline:
  query:
    when: $sdkKey != ''
    action: http/runner:send
    description: "system knowledge - Go error handling"
    requests:
      - Method: POST
        URL: ${serverURL}/v1/agent/query
        JSONBody:
          AgentID: knowledge_system
          Query: What are the Go error handling best practices?
          UserId: e2e-test
        Expect:
          Code: 200
          JSONBody:
            "@exists@conversationId": true
            content: /error/
