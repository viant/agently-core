pipeline:
  metadataAgentStarter:
    action: exec:run
    description: "metadata - backend exposes agent starter tasks under agentInfos"
    checkError: true
    commands:
      - ${repoPath}/e2e/scripts/check_metadata_agent_starter.py ${serverURL}
