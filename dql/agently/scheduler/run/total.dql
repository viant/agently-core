/* {"URI":"/total"} */

#define($_ = $Status<?>(output/status))
#define($_ = $Data<?>(output/view))
#define($_ = $Metrics<?>(output/metrics))

#define($_ = $HasScheduleID<bool>(const/value).Value('true').WithPredicate(0, 'expr', 't.schedule_id IS NOT NULL'))
#define($_ = $EffectiveUserID<string>(const/value).Value('').WithPredicate(0, 'expr', 'EXISTS (SELECT 1 FROM schedule s WHERE s.id = t.schedule_id AND (s.visibility IS NULL OR s.visibility <> ''private'' OR s.created_by_user_id = ?))'))
#define($_ = $ScheduleId<string>(query/scheduleId).WithPredicate(0, 'expr', 't.schedule_id = ?'))
#define($_ = $RunStatus<string>(query/status).WithPredicate(0, 'equal', 't', 'status'))

SELECT run_total.*
FROM (
    SELECT COUNT(*) AS record_count
    FROM run t
    ${predicate.Builder().CombineOr($predicate.FilterGroup(0, "AND")).Build("WHERE")}
) run_total
