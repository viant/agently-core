// Package config defines executor-level defaults and tuning knobs.
//
// This includes model/embedder defaults, runtime/storage paths, tool and
// agent auto-selection defaults, match/resource defaults, and timeout-related
// settings used by runtime composition.
package config
