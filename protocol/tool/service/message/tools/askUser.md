Ask the user a question and wait for their response.
Use this tool when you need specific information from the user that cannot be
inferred from context. Provide a clear message explaining what you need and
optionally a JSON Schema describing the expected response shape.

The tool blocks until the user responds (accept with payload) or declines.
The result contains the user's action and their response payload.
