{{- if .Context }}
Context:
{{ .Context }}
{{- end }}

Task:
{{ .Task.Prompt }}

Output:
- Update the DI document to append all newly completed items (do not remove previous items). Include timestamped summaries and references to files/functions verified.
- Save to the explicit DI path if provided; otherwise use the previously selected path for this session or an inferred default `<repo>/<feature>_di.md`.
- Return only the saved file location (absolute path) and a one‑line status; do not include the full document content unless explicitly requested.
